{
  "tabWidth": 4,
  "semi": true,
  "quoteProps": "consistent",
  "overrides": [
    {
      "files": ["*.json", "*.yaml", "*.yml", "*.md"],
      "options": {
        "tabWidth": 2
      }
    }
  ]
}
