load("//tools/nodejs:index.bzl", "jest_test", "lib", "prettier", "ts_project")

ts_project(
    srcs = glob(
        include = ["src/**/*.ts"],
        exclude = ["src/**/*.spec.ts"],
    ),
)

lib(deps = [":tsconfig"])

ts_project(
    name = "tsconfig_test",
    srcs = glob(["**/*.spec.ts"]),
    deps = [
        ":tsconfig",
        "@npm//@types/jest",
        "@npm//jest",
        "@npm//ts-jest",
    ],
)

jest_test(
    name = "unit",
    data = [":tsconfig_test"],
)

prettier()
